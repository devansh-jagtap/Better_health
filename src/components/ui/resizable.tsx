"use client"

// This component is currently not being used and has type compatibility issues
// with react-resizable-panels v4.x. Uncomment and fix when needed.

// Placeholder exports to prevent import errors
export const ResizablePanelGroup = () => null;
export const ResizablePanel = () => null;
export const ResizableHandle = () => null;
